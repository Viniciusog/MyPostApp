<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:editTextColor">@color/corPreta</item>
        <item name="android:textColorHint">@color/corPreta</item>
        <item name="android:textColor">@color/corPreta</item>
    </style>

    <style name="AppThemeLogin" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/corPreta</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeSearch" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->

        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/corPreta</item>
        <item name="colorControlNormal">@color/corPreta</item>
        <item name="colorControlActivated">@color/corPreta</item>
        <item name="android:editTextColor">@color/corPreta</item>
        <item name="android:textColorHint">@color/corCinzaEscuro</item>
        <item name="android:textColor">@color/corPreta</item>
    </style>

</resources>
